socket
threading
hashlib
logging
tqdm==4.66.1  # For progress bars in CLI
flask==2.3.3  # For web interface (bonus)
werkzeug==2.3.7  # For secure file handling with Flask
flask-login==0.6.2  # For user authentication (bonus)
cryptography==41.0.3  # For encryption of sensitive data (bonus